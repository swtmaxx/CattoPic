# Local development: point the frontend dev server at the local Worker.
NEXT_PUBLIC_API_URL=http://localhost:8787
NEXT_PUBLIC_REMOTE_PATTERNS=https://your-r2-domain.com

# Production uses the same Cloudflare Worker for the frontend and API.
# Leave NEXT_PUBLIC_API_URL empty for same-origin requests.
